# Copy to `.env` on the NAS and fill in. `.env` itself is gitignored.
#
# From Cloudflare Zero Trust → Networks → Tunnels → your tunnel → the install command.
# Treat it like a password: it authorises publishing traffic on your tunnel.
TUNNEL_TOKEN=
